# mod.io API key (read-only)
MODIO_API_KEY=

# Gmail settings
GMAIL_USER=k
GMAIL_APP_PASSWORD=
TO_EMAIL=

'''

PYTHONPATH="$(pwd)" python src/mod_alert.py

#cd ~/bg3-ps5-mod-alert
source .venv/bin/activate

# run once (prints "PS5 count = …", updates state.json)
python -m src.mod_alert

# set a baseline (use the number you want as the baseline)
python -m src.mod_alert --baseline 771

# loop every 30 minutes
python -m src.mod_alert --interval-mins 30

# send a test email
python -m src.mod_alert --test-email

# find the game id/slug (debug helper)
python -m src.mod_alert --find-game baldur


'''